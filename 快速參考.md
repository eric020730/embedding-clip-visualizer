# 🚀 快速參考卡

## 📝 每次修改的標準流程

```bash
# 1. 在 VS Code 中編輯檔案
# 2. 按 Ctrl+S 存檔
# 3. 在終端機執行以下指令：

git add .
git commit -m "說明你改了什麼"
git push

# 4. 等待 30 秒，Vercel 自動部署完成！
```

## ⚡ VS Code 必記快捷鍵

| 功能 | 快捷鍵 |
|------|--------|
| 儲存 | `Ctrl + S` |
| 開啟終端機 | `` Ctrl + ` `` |
| 搜尋 | `Ctrl + F` |
| 取代 | `Ctrl + H` |
| 註解 | `Ctrl + /` |
| Source Control | `Ctrl + Shift + G` |
| 格式化程式碼 | `Shift + Alt + F` |

## 🔧 常用 Git 指令

```bash
# 查看狀態
git status

# 查看修改
git diff

# 提交所有修改
git add .
git commit -m "訊息"
git push

# 查看歷史
git log --oneline

# 撤銷修改
git checkout .
```

## 🌐 你的網址

- **開發預覽**: http://localhost:5500 (Live Server)
- **正式網站**: https://你的專案名稱.vercel.app

## 📁 專案結構

```
embedding-visualizer/
├── index.html          ← 主要檔案（修改這個）
├── package.json        ← 專案資訊
├── vercel.json         ← Vercel 配置
├── README.md           ← 專案說明
└── .gitignore          ← Git 忽略檔案
```

## 🎨 快速修改指南

### 修改標題
在 `index.html` 搜尋：
```html
<h1 className="text-4xl...">
  Embedding & CLIP 視覺化  ← 改這裡
</h1>
```

### 修改資料點
搜尋 `embeddingData`：
```javascript
{ id: 1, x: 100, y: 100, z: 100, label: '貓', color: '#3B82F6' }
```

### 修改顏色
搜尋並替換：
- `#3B82F6` → 藍色
- `#EF4444` → 紅色
- `#10B981` → 綠色

## 🆘 遇到問題？

1. **無法 push**: 使用 GitHub Personal Access Token
2. **沒有更新**: 檢查 Vercel Dashboard
3. **語法錯誤**: VS Code 會用紅色波浪線標示

## 📞 取得幫助

- GitHub: https://github.com
- Vercel: https://vercel.com/docs
- VS Code: https://code.visualstudio.com/docs
